name: Build site

on:
    push:
        branches: [master]

jobs:
    build:
        runs-on: ubuntu-latest
        steps:
            - uses: actions/checkout@v3

            - name: Setup
              run: ./scripts/setup.sh

            - name: Lint
              run: ./scripts/lint.sh
